# To Do

- Implement a way to add optional uniform setting in the renderer (maybe `std::unordered_map`)
- Make the timer HUD more *clean*
- Add phong lighting

- For debug view, just move the camera's y axis up
