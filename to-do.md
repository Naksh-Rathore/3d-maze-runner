# To Do

- Fix camera bobbing snapping bug because glfwGetTime changes per second but the camera needs it to change per call
- Create a chest collection HUD

- For debug view, just move the camera's y axis up
